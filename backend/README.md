# Backend

Deterministic replay simulator backend for the LLM load balancer dashboard.

This README is used for packaging metadata in deployment builds. See the repo
root README for full project details.
